# 用 DDD 划边界：一个小项目的实操记录

> 这篇讲的是**战略层的 DDD**——怎么用子域划分决定"什么该做透、什么该点到为止、
> 什么根本不该碰核心"。不涉及实体、聚合根、仓储那套战术模式。
> 案例来自一个个人知识库项目，但方法本身与技术栈无关。

## 一、先解决一个误会：DDD 不等于那套战术模式

提到 DDD，大部分人想到的是 Entity、Value Object、Aggregate Root、Repository。
这些属于**战术设计**，它们解决的是"一个已经划好的模块内部怎么组织代码"。
对小项目来说，战术模式的性价比很低——你没有那么复杂的业务规则，
硬套只会得到一堆转发式的样板代码。

真正对小项目有用的，是**战略设计**，核心就三个概念：

- **子域（Subdomain）**——问题空间的切分。你的业务由哪几块构成，各自多重要。
- **限界上下文（Bounded Context）**——解空间的切分。代码里一块有明确边界、
  内部术语自洽的模块。
- **上下文映射（Context Mapping）**——上下文之间怎么互相调用、谁依赖谁。

一句话概括战略设计要回答的问题：**我的力气该往哪儿使，以及哪些地方允许糊弄。**

这对一个人的项目尤其重要。人手是最稀缺的资源，而"什么都想做好"是最贵的错误。

## 二、三档子域：核心 / 支撑 / 通用

标准划分是三档，各自对应完全不同的投入策略：

**核心域（Core Domain）**——你的竞争力所在，拿掉产品就不成立的部分。
资源优先砸这里，允许过度设计，允许反复重构。

**支撑子域（Supporting Subdomain）**——业务需要，但不构成差异化。
够用就停。这里写出"不够优雅"的代码是正确决策，不是技术债。

**通用子域（Generic Subdomain）**——认证、对象存储、发邮件、定时任务，
谁做都一样。**这一档的唯一正确动作是用现成的。**自研通用子域是小项目最常见的
死法：投入巨大，产出为零，而且再也放不下。

第一个关键点：**核心域的判据是"差异化"，不是"难"，也不是"大"。**

分布式调度、消息队列、权限引擎——这些技术上很难、代码量很大，但如果它们不是你
区别于竞品的原因，它们就不是核心域。反过来，你的核心域可能只是几百行代码，
但那几百行是别人抄不走的东西。

## 三、划分的三个判据

理论谁都会念，落到具体功能上还是会卡。我最后收敛成三条，按顺序问，
第一个"是"就定档：

**1. 它要不要修改核心数据模型？**
要 → 它是核心域。这条是反向验证：如果你直觉认为某功能是外围，但它非改核心模型
不可，那么要么你分类错了，要么方案错了。**这是三条里唯一有硬约束力的一条**，
也是实践中最有用的一条——它能拦住绝大多数"顺手把系统搞乱"的需求。

**2. 有没有别的能力依赖它？**
有 → 核心域。被依赖的东西不可能是外围，因为它已经是别人的地基了。

**3. 拿掉它，系统是否仍然自洽？**
自洽 → 支撑域。这条是终极检验：能想象一个没有它的版本仍然成立，它就是可选的。

## 四、最容易犯的错：把边界划在功能上

这是我认为整套方法里最反直觉、也最有价值的一条经验。

划分子域时，人的本能是按"功能"整块归类：博客发布是外围，那就整块归到外围。
**这几乎总是错的。**

我的项目里，"对外发布"这个功能拆开看是两半：

- 「一篇文档能不能被匿名访问」——这是**权限模型的一部分**，是核心。
  它有必须守住的不变量（未发布的内容任何路径都不可达），一旦作为外围功能随手
  实现，就是一个安全漏洞。
- 「它在公开站点上长成什么样」——皮肤、导航、排版，这是**支撑域**。
  整套换掉，系统照常自洽。

如果整块归到外围，权限那半就失去了应有的严格性；整块归到核心，就要为一套随时
可能推翻的皮肤付出核心域的维护成本。

同样的结构出现在检索功能上：索引与写入路径的挂钩是核心（拔掉会破坏写入链路），
搜索/问答的界面是支撑（随便换）。

**推论：边界经常划在一个功能的内部，而不是功能之间。**
判断的时候不要问"这个功能是核心还是外围"，要问"这个功能里，哪部分承载了不可
违反的不变量"。承载不变量的那部分，无论多小，都是核心。

## 五、从子域到代码：一条规则

子域是问题空间的划分，代码里对应的是限界上下文。理想情况一对一，但不必强求，
小项目里一个上下文就是一个目录/包。

上下文之间的关系（上下文映射）在小项目里可以只保留最简单的一种：

> **外围可以依赖核心，核心不得知道外围存在。**

这条规则的判据要比 `import` 宽——**核心代码里出现外围的名词**（类型名、
接口方法名、字段名、枚举值），即使没有 import，也算越界。核心一旦开始认识外围
的概念，边界就已经破了，只是还没痛。

由此派生出一个实践中反复用到的手法：**外围需要核心配合时，核心提供的是中性的
接缝，不是针对某个外围功能的分支。**

举我项目里的实例。公开站点复用了知识库的文档渲染器，但有两处行为必须变：
标签点击应当失效（匿名读者没有可筛选的列表），站内链接要重定基（站点可能挂在
子路径下）。

错误做法是在渲染器里写 `if (isBlogSite) { ... }`——核心从此认识 blog 了。

实际做法是在渲染器里放一个**可空的上下文**：填了就按"只读外链模式"渲染，
不填就是普通文档。核心只知道存在这样一种模式，不知道是谁在用。外围通过注入
把自己的需要传进来。

这就是接缝：**核心暴露的是能力的维度，不是使用者的身份。**

（顺带一提，我这个接缝的命名里带了外围功能的名字，这仍是个瑕疵——严格说应该
叫中性名字。不影响正确性，但它说明边界侵蚀往往先从命名开始。）

## 六、边界的三种长相

在自己的代码里，边界的健康程度有三种典型形态。学会识别它们，比记住任何定义
都有用。

**接缝（seam）——正确的形态。**
核心开一个中性的可选扩展点，外围填充。核心不认识外围。上一节的例子。

**渗透（leak）——灰色地带。**
外围的概念进入了核心的公共契约。我项目里的实例：一个集中的存储接口里直接列着
十几个只服务于发布功能的方法。它没有 bug，编译通过，测试全绿，但核心已经认识
外围的名词了。

渗透的特点是**不痛**，所以会持续累积，直到某天你想拆的时候发现拆不动。
现实的处理方式通常不是回滚（收益不抵风险），而是**止损**：承认现状，
约定不再往这里加新的。

**影子耦合（shadow coupling）——最危险的形态。**
两处代码靠注释约定读同一份数据格式，互相"镜像"，但没有任何代码级的依赖。
我项目里就有一处：两个模块各自解析同一份存储的 JSON，注释里写着"与某某文件的
解析互为镜像"。

它比显式依赖危险得多——**编译器看不见，lint 看不见，重构时没有任何提示，
改一边忘一边就是线上行为分叉。**唯一的解法是让那份约定有单一出处，
谁需要谁引用，而不是各抄一份。

## 七、边界怎么反作用于需求

划分子域最实际的收益，其实不在代码，在**需求管理**。

每个需求进来先定档，档位直接决定四件事：

| | 核心域 | 支撑子域 |
|---|---|---|
| 抽象 | 值得，会长期演化 | 别抽象，直写，先跑通 |
| 测试 | 完整覆盖，尤其是不变量 | 主路径即可 |
| 数据模型 | 可演化 | **不得修改** |
| 完成度 | 做透 | 够用就停 |

前两行和最后一行是资源分配的默认值，第三行是硬约束。

但真正的价值是心理层面的：**它给"这个先不做/做糙一点"提供了一个不必反复论证
的理由。**没有这套划分的时候，每个"要不要做完善"的决定都要重新纠结一遍，
而且往往因为"反正也不难"就做了。有了档位，答案是预先约定好的。

对一个人的项目，这可能是这套方法最大的收益——**它不是让你写出更好的代码，
是让你有底气少写代码。**

## 八、边界靠机器守，不靠记性

规则写在文档里等于没写。依赖方向这条规则的好处是**它可以被静态检查**：

- Go 侧：`golangci-lint` 的 `depguard`，支持按文件路径配 deny 规则，
  表达"核心包不得 import 外围包"。
- 前端：eslint 的 `no-restricted-imports`，或 Biome 的 `overrides` +
  `noRestrictedImports`，按目录限制 import 来源。

都是配置级改动，几十行，一次配好长期有效。

但要清楚它的**盲区**：静态检查只能拦住显式依赖，拦不住"渗透"（命名层面的
认识），更拦不住"影子耦合"。这两类只能靠评审时的一句追问：
**"这个概念为什么会出现在核心里？"**和**"这份约定的单一出处在哪？"**

## 九、这套方法什么时候会失效

三个诚实的局限：

**分类会漂移。**Core Domain 不是永恒的。今天的支撑域可能因为用户反馈成为明天
的差异化能力，今天的核心域可能因为行业标准化沦为通用域（认证就是典型的、
已经完成这个迁移的例子）。所以分类结果必须是**常青文档**，随系统原地改写，
而不是一次划定终身有效。

**它有持续的判断成本。**每个需求都要定档，每次核心要加东西都要问一句"这个概念
属于核心吗"。这个摩擦是有意为之的，但它确实是成本。项目小到一定程度
（几千行、一个功能），这个成本大于收益，不如不划。

**它治不了动机问题。**如果你就是想把那个外围功能做得很酷，分类不会阻止你。
它只能保证你在越界时是**知情的**——你知道自己在为一个支撑域付核心域的价钱。
知情的过度投入不是坏事，无意识的才是。

---

## 一页纸总结

1. 小项目要的是 DDD 的**战略设计**，不是战术模式。
2. 三档：核心（砸资源）/ 支撑（够用就停）/ 通用（**一律用现成的**）。
3. 核心域的判据是**差异化**，不是难度或代码量。
4. 定档三问：改不改核心模型 / 有没有人依赖 / 拿掉是否自洽。
5. **边界经常在功能内部**——承载不变量的那部分是核心，哪怕它只有几十行。
6. 代码规则只有一条：**外围依赖核心，核心不认识外围**。判据宽于 import，
   出现名词就算越界。
7. 核心暴露**能力的维度**，不是使用者的身份——这就是"接缝"。
8. 三种长相：接缝（好）/ 渗透（不痛，会累积，要止损）/ 影子耦合（编译器看不见，
   最危险）。
9. 最大收益在需求侧：**有底气少写代码。**
10. 用 lint 守显式依赖，用评审的两句追问守剩下的。

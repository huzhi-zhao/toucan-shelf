# temp folder
tmp

# Frontend asset
web/dist

# Build artifacts
build/
bin/
# The compiled binary at the repo root. Anchored: a bare `memos` also matches the
# cmd/memos directory, which silently hides new files added there.
/memos

.DS_Store

# Jetbrains
.idea

# Docker Compose Environment File
.env

dist

# VSCode settings
.vscode

# Git worktrees
.worktrees/

# Local pnpm store (project-scoped, created when --config.store-dir is set
# without an existing store; contains a symlink back to the workspace).
.pnpm-store/

# Frontend test coverage output (Vitest + @vitest/coverage-v8).
web/coverage/
/memos_prod.db
/memos_prod.db-shm
/memos_prod.db-wal
/.thumbnail_cache/
/assets/**.jpg

# memogit CLI build artifact
/memogit

# design canvas: generated payload (~2.5MB) and the logo copy the seeder needs
/.poster/toucanshelf-poster.html
/.poster/logo.svg

// The mark toolbar is shared with notebook documents (see components/MarkToolbar); re-exported
// under the name the EPUB reader already uses, to keep its call sites unchanged.
export { MarkToolbar as EpubMarkToolbar } from "@/components/MarkToolbar";
